/**
 * Deliberately in the same pull request as its spec, which is the violation this
 * branch exists to demonstrate. Never merged.
 */
export function DemoWidget() {
  return <span data-slot="demo-widget" />;
}
