# PR-1 — <Component> spec

Contains the decision document and, if one was made, the visual draft. **No implementation.**

## Visual
<The published story preview link from this pull request's CI comment, or screenshots of the
component in its main states.>

> Required. A reviewer approving a spec they have not seen rendered is approving prose — and
> prose is what post-freeze visual revisions spend their time rewriting.

## Draft reuse
<If a draft exists: which of its decisions the spec adopted, and which it rejected.
If no draft was made: one line saying why.>

## Requirements covered
| Requirement | Where the spec addresses it |
|---|---|
| R1 | §… |

## Acceptance criteria
- [ ] Every numbered requirement from the brief is addressed or explicitly deferred
- [ ] Accessibility contract decided, not deferred to implementation
- [ ] Every token reference resolves to an existing token
- [ ] Edge cases evaluated per archetype
- [ ] `lifecycle` is `freeze_candidate` — never `frozen`; the merge is the freeze

## Notes for the reviewer
<Anything genuinely undecided. Say so here rather than leaving it implicit in the spec.>
